fastapi==0.115.6
uvicorn[standard]==0.32.1
psycopg[binary,pool]==3.2.3
pydantic==2.10.4
python-dotenv==1.0.1
Pillow==11.2.1
# RAGFlow Python SDK для генерации статей события. Желательно совпадение minor-версии
# с вашим RAGFlow-сервером. Импортируется лениво — нужен только при EVENT_RAGFLOW_ENABLED=true.
ragflow-sdk==0.20.5
